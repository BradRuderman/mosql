upcounsel:
  users:
    :columns:
      ServicesRate:
        :source: ServicesRate
        :type: TEXT
    :extract:
      - BarCerts
      - b.a.BarCerts
    :meta:
     :table: users
     :extra_props: true
